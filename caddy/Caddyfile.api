# API-only HTTPS for api.aitrads.in (frontend stays on aitrads.in File Manager).
# If browser shows ERR_SSL_PROTOCOL_ERROR: wipe caddy volumes and rebuild
# (see upload/BUILD.txt).

{
	email {$ACME_EMAIL:admin@aitrads.in}
}

api.aitrads.in {
	encode gzip
	reverse_proxy backend:8000
}
