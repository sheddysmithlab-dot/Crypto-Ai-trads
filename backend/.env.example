# Copy to backend/.env and fill in. Never commit .env.

BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=false
BYBIT_ACCOUNT_TYPE=UNIFIED

# Optional overrides (defaults match main.py / trading_policy.py)
# SYMBOL=BTCUSDT
# CATEGORY=linear
# INTERVAL=1
# LEVERAGE=10
# POSITION_USDT=10
# MAX_OPEN_TRADES=3
# TAKE_PROFIT_PCT=0.8
# STOP_LOSS_PCT=0.4
# PAPER_MODE=true
# WHALE_POLL_SECONDS=60
# WHALE_MIN_BTC=100
# WHALE_ALERT_ENABLED=true

# Session schedule (Mon–Fri IST auto AI on/off). UI switch also persists to backend/data/.
# SESSION_SCHEDULE_ENABLED=false

# Hostinger website MySQL (Trading Statement). Not a VPS mysql container.
# hPanel → Databases → Remote MySQL: whitelist VPS public IP.
MYSQL_ENABLED=true
MYSQL_HOST=srv1668.hstgr.io
MYSQL_PORT=3306
MYSQL_USER=u808821982_aitrads
MYSQL_PASSWORD=
MYSQL_DATABASE=u808821982_aitrads

# Cost-aware gate (PDF plan — mid discipline)
COST_AWARE_ENABLED=true
COST_AWARE_DRY_RUN=false
COST_AWARE_LAMBDA=1.2
COST_AWARE_MIN_CANDLE_RANGE=0.25
COST_AWARE_ABS_MIN_RANGE_PCT=0.02
COST_AWARE_ROUNDTRIP_COST_PCT=0.11

# Fire discipline (Bible allowlist + 3-bar gap)
MIN_PATTERN_STRENGTH=0.75
MIN_BARS_BETWEEN_AUTO_ENTRIES=3
BLOCK_OPPOSITE_AUTO_SIDE=true
VOLUME_CONFIRM_MULT=1.6
VOLUME_VS_PREV_MULT=1.15
CANDLE_TO_VOLUME_MULT=0
STRICT_EXIT_MIN_LOCK=0.40
STRICT_EXIT_HARD_TARGET=1.2
