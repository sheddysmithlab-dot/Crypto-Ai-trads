# Backend URL used at build time in production (e.g. on Render).
# Leave unset for local development - the app auto-detects localhost/LAN IP instead.
VITE_API_BASE_URL=https://ai-agent-bybit-backend.onrender.com
